properties([disableConcurrentBuilds()])

stage('Build') {
    node ('Ruby') {
            echo 'Building...'
            checkout scm
            sh "ip address"
            sh 'env > env.txt' 
            for (String i : readFile('env.txt').split("\r?\n")) {
                     println i
            }
    }    
}
