properties([disableConcurrentBuilds()])

stage('Build') {
    node ('Ruby') {
            echo 'Building...'
            def scmVars = checkout scm
            def branchName = scmVars.GIT_BRANCH
            echo branchName
            sh "ip address"
            echo scmVars
    }    
}
