pipeline {
    agent { label 'master' }
    options {
      timestamps()
    }
    stages {
        stage('Build') {
            agent {label 'Ruby'}
            when { 
                changeRequest() 
            }
            steps {
                echo "Building...  ${pullRequest.id}"
                sh "ip address"
            }
        }
        stage('Test on Linux') {
            steps {
                 sh("printenv")
            }
        }
        stage('Test on Windows') {
            agent {
                label 'Ruby'
            }
            steps {
               echo 'testing'
               sh("printenv")
               sh "whoami"
               echo 'deploying'
               sh "git branch"
            }
        }
    }
}
