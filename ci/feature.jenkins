properties([disableConcurrentBuilds()])

stage('Build') {
    node ('Ruby') {
            echo 'Building...'
            def scmVars = checkout scm
            def branchName = scmVars.CHANGE_TARGET
            echo branchName
            sh("printenv")
    }    
}
